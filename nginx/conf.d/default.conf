limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=api:10m rate=100r/m;

server {
    listen 80;
    server_name api.altisonelabz.com localhost;

    location /api/v1/auth/login {
        limit_req zone=login burst=3 nodelay;
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name altisonelabz.com;
    root /usr/share/nginx/html/landing;
    try_files $uri /index.html;
}

server {
    listen 80;
    server_name admin.altisonelabz.com;
    root /usr/share/nginx/html/admin;
    try_files $uri /index.html;
}

server {
    listen 80;
    server_name lms.altisonelabz.com;
    root /usr/share/nginx/html/lms;
    try_files $uri /index.html;
}

server {
    listen 80;
    server_name mentor.altisonelabz.com;
    root /usr/share/nginx/html/mentor;
    try_files $uri /index.html;
}
